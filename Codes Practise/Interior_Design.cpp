#include <iostream>
using namespace std;
#include <math.h>
#include <stdio.h>
// #include <bits/stdc++.h>

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int a, b, c, d;
        cin >> a >> b >> c >> d;
        int x = a + b;
        int y = c + d;
        if (x > y)
        {
            cout << y << endl;
        }
        else
        {

            cout << y << endl;
        }
    }
    return 0;
}
