#include <iostream>
using namespace std;
#include <math.h>
#include <stdio.h>
#include <bits/stdc++.h>

int main()
{


    cout<<"hello Neehar";
    // int n;
    // cin >> n;
    // int a[n];
    // int max = 0, max2 = 0;
    // for (int i = 0; i < n; ++i)
    // {
    //     cin >> a[i];

    //     if (a[i] > max)
    //     {
    //         max = a[i];
    //     }
    //     if (a[i] > max2 && a[i] < max2)
    //     {
    //         max2 = a[i];
    //     }
    // }

    return 0;
}